## Packages
framer-motion | Smooth animations for gacha pulls and battle logs
clsx | Class name merging
tailwind-merge | Utility for merging tailwind classes safely

## Notes
- Expecting Replit Auth to handle session management via /api/login and /api/logout.
- Backend provides endpoints defined in the schema for game actions.
- Static images will be sourced from Unsplash for thematic backgrounds.
